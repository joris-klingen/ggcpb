% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{save_cpb}
\alias{save_cpb}
\title{Save a plot at CPB page dimensions}
\usage{
save_cpb(
  filename,
  plot = ggplot2::last_plot(),
  page = c("half", "full"),
  preset = c("report", "presentation"),
  height = NULL,
  width = NULL,
  dpi = 300,
  device = ragg::agg_png,
  bg = cpb_bg,
  ...
)
}
\arguments{
\item{filename}{Path to write to; passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{plot}{The plot to save; defaults to \code{\link[ggplot2:last_plot]{ggplot2::last_plot()}}.}

\item{page}{Either \code{"half"} (default, 2.98 in wide) or \code{"full"}
(5.96 in wide). Ignored if \code{width} is supplied explicitly.}

\item{preset}{Either \code{"report"} (default, 2.98 in tall) or
\code{"presentation"} (2.5 in tall). Ignored if \code{height} is supplied
explicitly.}

\item{height}{Explicit height in inches. \code{NULL} (default) uses
\code{preset} to determine the height.}

\item{width}{Explicit width in inches; must be \code{2.98} or \code{5.96}.
\code{NULL} (default) uses \code{page} to determine the width.}

\item{dpi}{Resolution in dots per inch; defaults to \code{300}. CPB tall
exports commonly use \code{dpi = 800}.}

\item{device}{Graphics device passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}};
defaults to \code{\link[ragg:agg_png]{ragg::agg_png()}} so the bundled CPB font renders
correctly.}

\item{bg}{Output background colour; defaults to the CPB background
colour so it matches \code{theme_cpb()}'s on-plot background. Use
\code{bg = NA} for a transparent background.}

\item{...}{Further arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}
}
\value{
Invisibly, the \code{filename} that was written.
}
\description{
A wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} that enforces the CPB page
widths and renders with the \code{ragg} device by default (needed for the
bundled \code{RijksoverheidSansText} font to render correctly).
}
\details{
Width is strict: it is set by \code{page}, not free-form. \code{page = "half"}
gives a width of 2.98 in; \code{page = "full"} gives 5.96 in. An explicit
\code{width} is only an escape hatch and is validated against these two
values -- any other width errors, so a stray \code{width = 8} fails
loudly rather than silently producing an off-spec figure.

Height defaults to 2.98 in (the \code{"report"} preset). Pass
\code{preset = "presentation"} for the 2.5 in presentation height, or set
\code{height} explicitly for anything else (e.g. a tall stacked-facet
export) -- an explicit \code{height} always wins over \code{preset}.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(factor(cyl))) +
  geom_bar() +
  theme_cpb()
save_cpb("cyl_bar.png", p, page = "half")
save_cpb("cyl_bar_full.png", p, page = "full", preset = "presentation")
}
}
