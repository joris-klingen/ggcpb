% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales.R
\name{scale_fill_cpb_manual}
\alias{scale_fill_cpb_manual}
\alias{scale_colour_cpb_manual}
\alias{scale_color_cpb_manual}
\title{CPB manual fill/colour scales, selecting specific palette positions}
\usage{
scale_fill_cpb_manual(
  index = NULL,
  palette = c("qualitative", "discr", "sequential", "blues"),
  na.value = cpb_na,
  ...
)

scale_colour_cpb_manual(
  index = NULL,
  palette = c("qualitative", "discr", "sequential", "blues"),
  na.value = cpb_na,
  ...
)

scale_color_cpb_manual(
  index = NULL,
  palette = c("qualitative", "discr", "sequential", "blues"),
  na.value = cpb_na,
  ...
)
}
\arguments{
\item{index}{Integer vector of positions into \code{palette}, in the
order the resulting scale should use them. If \code{NULL} (default),
the full palette is used in its original order.}

\item{palette}{One of \code{"qualitative"} (default), \code{"discr"},
\code{"sequential"} (pink ramp), or \code{"blues"} (blue ramp).}

\item{na.value}{Colour used for \code{NA} values; defaults to the CPB NA
colour (\code{"lightgrey"}).}

\item{...}{Passed through to \code{\link[ggplot2:scale_manual]{ggplot2::scale_fill_manual()}} /
\code{\link[ggplot2:scale_manual]{ggplot2::scale_colour_manual()}}.}
}
\value{
A ggplot2 \code{Scale} object.
}
\description{
A \verb{scale_*_manual()} built from specific, ordered positions in a CPB
palette, for the common case where a plot needs a small, deliberately
ordered subset of the palette (e.g. \code{cpb_colors[c(6, 2)]} in CPB
source scripts, or \code{cpb_colors_scale[5:1]} for a reversed subset of
the sequential ramp).
}
\examples{
library(ggplot2)
ggplot(mpg, aes(class, fill = drv)) +
  geom_bar() +
  scale_fill_cpb_manual(index = c(6, 2, 5))
}
