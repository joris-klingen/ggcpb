% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{cpb_box}
\alias{cpb_box}
\title{A CPB-styled quantile box-and-errorbar chart}
\usage{
cpb_box(
  data,
  x,
  p5,
  p25,
  p50,
  p75,
  p95,
  mean = NULL,
  fill = NULL,
  fill_colour = NULL,
  group = NULL,
  group_gap = 0.7,
  box_style = c("ggcpb", "james", "modern", "dot"),
  dot_labels = NULL,
  box_labels = NULL,
  label_accuracy = 0.1,
  width = 0.5,
  linewidth = 0.25,
  palette = "qualitative",
  fill_index = NULL,
  index = NULL,
  pct_axis = FALSE,
  value_accuracy = NULL,
  value_breaks = NULL,
  value_limits = NULL,
  value_axis = c("bottom", "top"),
  x_lim = NULL,
  x_lim_follow_data = TRUE,
  orientation = c("vertical", "horizontal"),
  sec_y = NULL,
  sec_type = c("line", "point", "col"),
  sec_limits = NULL,
  sec_label = NULL,
  sec_ylab = NULL,
  sec_colour = NULL,
  sec_linewidth = 0.55,
  sec_points = FALSE,
  sec_point_size = 1.6,
  sec_col_width = 0.3,
  sec_accuracy = NULL,
  facet = NULL,
  facet_ncol = NULL,
  facet_scales = "fixed",
  legend = "bottom",
  reverse_legend = FALSE,
  legend_ncol = NULL,
  zeroline = NULL,
  minor = FALSE,
  ticks = TRUE,
  flush_legend = TRUE,
  axis_text_size = 7,
  legend_key_size = NULL,
  grid_colour = "black",
  grid_linewidth = 0.1,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  filllab = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table with one row per box, holding
precomputed quantile columns.}

\item{x}{Column mapped to the x aesthetic (tidy eval), i.e. the
category each box belongs to.}

\item{p5, p25, p50, p75, p95}{Columns holding the precomputed 5th,
25th, 50th (median), 75th and 95th percentiles (tidy eval).}

\item{mean}{Optional column holding the mean of each distribution
(tidy eval), drawn as a diamond marker. Only used by
\code{box_style = "dot"}; ignored by the box-drawing styles, which have
no published mean marker.}

\item{fill}{Optional column mapped to the fill aesthetic (tidy
eval), e.g. for grouped boxes side by side. Only supported by
\code{box_style = "ggcpb"}.}

\item{fill_colour}{Constant box fill used when no \code{fill} column is
mapped. Defaults to \code{NULL}, which resolves to the CPB primary blue
(\code{cpb_cols(6)}, \code{"#005faf"}) for \code{"ggcpb"}/\code{"james"} and the CPB
light blue (\code{cpb_cols(5)}, \code{"#87d2ff"}) for \code{"modern"}. Ignored
when \code{fill} is supplied. For the \code{"james"}/\code{"modern"} styles it
may also be a \emph{vector} with one colour per row of \code{data} (e.g.
one colour per \code{group}), recycled if shorter.}

\item{group}{Optional column (tidy eval) assigning each \code{x} category
to a group, for the published vertically grouped layout: every
group gets its name as a bold heading row on the category axis
above its categories, all boxes share one value axis. A group
containing exactly one category with the same name as the group
collapses onto its heading row (e.g. an "Alle huishoudens" total).
Combines with a \code{fill} mapping (\code{"ggcpb"} style): pass a
\code{position_dodge()} for e.g. two dodged years per category under
the group headings. Typically used with
\code{orientation = "horizontal"}. The category rows keep the house
category ticks; the bold headings carry none and are outdented.}

\item{group_gap}{Extra gap between group blocks, in category
widths; defaults to \code{0.7}.}

\item{box_style}{How the boxes are constructed:
\itemize{
\item \code{"ggcpb"} (default): the style already used in CPB
distributional figures -- capped errorbar whiskers plus an
outlined box with a median line.
\item \code{"james"}: the legacy \code{nplot()} box -- a borderless filled box,
plain (capless) whiskers in the box colour, a black median line
extending slightly beyond the box, and the median value printed
above it.
\item \code{"modern"}: the designer variant of \code{"james"} -- light-blue box
and whiskers, a thick dark-blue median line, the median value
in bold above it and the p25/p75 values printed below the box
ends.
\item \code{"dot"}: the marker variant used for survey distributions --
no box at all. A dashed connector spans p5-p95 with a light dot
at each end, a capped bar spans the p25-p75 range, a filled dot
marks the median and (when \code{mean} is supplied) a diamond marks
the mean. Unlike the other styles it draws a legend naming each
marker, so the reader can tell the five statistics apart.
}

\code{"james"}, \code{"modern"} and \code{"dot"} follow the house convention of
horizontal boxes; combine them with
\code{orientation = "horizontal"}.}

\item{dot_labels}{Legend labels for \code{box_style = "dot"}, as a named
character vector with elements \code{p5}, \code{iqr}, \code{p50}, \code{p95} and
\code{mean}. \code{NULL} (default) uses the published Dutch labels. Supply
only the elements you want to change; the rest keep their
defaults. Ignored by the other styles.}

\item{box_labels}{Whether to print the value labels of the
\code{"james"}/\code{"modern"} styles. \code{NULL} (default) resolves by
\code{box_style} (\code{TRUE} for \code{"james"}/\code{"modern"}, which always
ignore it under a \code{fill} mapping); ignored for \code{"ggcpb"}.}

\item{label_accuracy}{Rounding of the printed value labels, passed
to \code{\link[=label_number_nl]{label_number_nl()}}; defaults to \code{0.1} (one decimal, Dutch
comma).}

\item{width}{Box width; the errorbar width is drawn at half this
value. Defaults to \code{0.5}.}

\item{linewidth}{Stroke width of the box outlines, median line and
errorbars in the \code{"ggcpb"} style. Defaults to \code{0.25}, matching
the thin strokes of the published CPB distributional figures.}

\item{palette}{CPB palette to use for \code{fill}; one of
\code{"qualitative"} (default), \code{"discr"}, \code{"sequential"}
(pink ramp), or \code{"blues"} (blue ramp).}

\item{fill_index}{Which house colours the series get. Either a vector
of palette positions -- \code{c(2, 5, 6)}, forwarded to
\code{\link[=scale_fill_cpb_manual]{scale_fill_cpb_manual()}} -- or a keyword naming a palette:
\code{"discrete"} for the qualitative house palette (blue, magenta,
taupe, ...) and \code{"continuous"} for the sequential ramp. \code{NULL}
(default) uses \code{palette}, which is \code{"discrete"} for every wrapper
except \code{\link[=cpb_map]{cpb_map()}}. A keyword and a non-matching \code{palette} are a
conflict and raise an error, since both set the same thing.}

\item{index}{Deprecated. Former name of
\code{fill_index}. Still accepted, with a warning.}

\item{pct_axis}{If \code{TRUE}, format the value axis with
\code{\link[=label_pct_nl]{label_pct_nl()}}.}

\item{value_accuracy}{Rounding accuracy for the value axis labels,
passed to \code{\link[=label_number_nl]{label_number_nl()}} (e.g. \code{0.1} for one decimal place).
\code{NULL} (default) lets \code{scales} pick a sensible accuracy from the
breaks. Cannot be combined with \code{pct_axis}. Use this instead of
adding a second \code{scale_y_continuous()}, which would discard the
wrapper's flush axis (see \code{value_breaks}).}

\item{value_breaks}{Optional breaks for the value axis (passed to
the wrapper-built \code{\link[ggplot2:scale_continuous]{ggplot2::scale_y_continuous()}}). Use this
instead of adding a second y scale, which would discard the
wrapper's axis formatting and expansion.}

\item{value_limits}{Optional length-2 numeric vector giving the
value-axis range, applied as the wrapper-built value scale's own
\code{limits} (not a coordinate-system zoom) -- the hard bound the axis
is drawn flush to; a box/whisker outside it is genuinely dropped,
with a warning, the same as setting \code{limits} on any ggplot2 scale.
\code{NULL} (default) flushes to the full p5-p95 (and \code{mean}) range
instead.}

\item{value_axis}{Where the value axis is drawn: \code{"bottom"}
(default) or \code{"top"}. \code{"top"} places the numeric scale along the
top of the panel, the convention of the CPB koopkracht figures;
it applies to horizontal boxes (the value axis is the flipped
axis).}

\item{x_lim}{Optional length-2 vector zooming the category (\code{x})
axis to a range, without dropping data -- applied as a
coordinate-system zoom (\code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} /
\code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}} \code{xlim}). \code{NULL} (default) shows the full
range.}

\item{x_lim_follow_data}{If \code{TRUE} (default), the category axis
sits flush to the data's own range, with no padding on either
side. A whole-number \code{x} (almost always a year) still only ever
gets whole-number breaks, never a fractional one. Set to \code{FALSE}
to restore ggplot2's usual padded, evenly spaced margin instead.
Matches nicerplot's parameter of the same name. Ignored when
\code{x_lim} is set, and when \code{group} is mapped (the grouped layout
needs its own fixed margin for the heading rows).
Adding your own \code{scale_x_continuous()}/\code{scale_x_discrete()}
afterward replaces this one entirely (ggplot2 keeps only one
scale per aesthetic) -- add \code{expand = ggplot2::expansion(mult = 0)}
to it to keep the flush behaviour.}

\item{orientation}{\code{"vertical"} (default) or \code{"horizontal"} (adds
\code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}} and is forwarded to \code{\link[=theme_cpb]{theme_cpb()}}).}

\item{sec_y}{Optional column (tidy eval) holding a series to draw
against a \strong{secondary value axis} on the right, alongside the
boxes. One value per \code{x}. Only supported for vertical boxes with
no \code{group} mapping, \code{box_style} other than \code{"dot"}, and
\code{value_axis = "bottom"} -- each of those already claims the space
\code{sec_y} needs (the right/top edge of the panel, or the colour
aesthetic). With no \code{fill} mapping the boxes get their own legend
key too (a plain square in \code{fill_colour}), so both series are
always named in the legend.}

\item{sec_type}{How \code{sec_y} is drawn: \code{"line"} (default), \code{"point"}
(markers only, no connecting line), or \code{"col"} (thin bars).}

\item{sec_limits}{Length-2 numeric vector giving the range the
secondary axis spans. \code{NULL} (default) uses zero to the maximum of
\code{sec_y}. \code{sec_y} is placed by mapping this range linearly onto
the primary (p5-p95) range, so the two axes always start together.}

\item{sec_label}{Legend label for \code{sec_y}. \code{NULL} (default) uses
the \code{sec_y} column name. Automatically suffixed \code{"(rechteras)"}
(right axis) -- don't add it yourself, e.g. \code{sec_label = "erfbelasting"} shows as \code{"erfbelasting (rechteras)"}.}

\item{sec_ylab}{Unit caption for the secondary axis, drawn
right-aligned above the panel to mirror the left-hand unit that
\code{ylab} puts in the subtitle. \code{NULL} (default) draws none.}

\item{sec_colour}{Colour for \code{sec_y}; defaults to \code{NULL}, which
resolves to the CPB pink (\code{cpb_cols(2)}, \code{"#e6006e"}).}

\item{sec_linewidth}{Line width; only used when \code{sec_type = "line"}.
Defaults to \code{0.55}.}

\item{sec_points}{If \code{TRUE}, add a marker at every point of the
\code{sec_y} line. Only used when \code{sec_type = "line"} -- for markers
without a connecting line, use \code{sec_type = "point"} instead.}

\item{sec_point_size}{Point size; only used when \code{sec_type = "point"}
(the main marker) or \code{sec_type = "line"} with \code{sec_points = TRUE}
(a smaller marker decorating the line, at 0.7x this). Defaults
to \code{1.6}.}

\item{sec_col_width}{Column width; only used when \code{sec_type = "col"},
drawn narrower than the primary bars' own default width (about
\code{0.9}) so the two do not simply overlap. Defaults to \code{0.3}.}

\item{sec_accuracy}{Rounding accuracy for the right-hand axis's own
labels, passed to \code{\link[=label_number_nl]{label_number_nl()}}. \code{NULL} (default) uses that
function's own automatic rounding -- set this when \code{sec_y} needs
a different precision than its default (e.g. whole numbers for a
count alongside a one-decimal percentage share).}

\item{facet}{Optional column (tidy eval) to facet by. Facets follow
the house (legacy nicerplot) convention: the facet title is a bold
strip \emph{below} each panel, and every panel is a complete
mini-figure with its own axes and axis labels.}

\item{facet_ncol}{Number of facet columns, passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{facet_scales}{Whether facet axis ranges are shared; passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} (\code{"fixed"} default, or \code{"free"},
\code{"free_x"}, \code{"free_y"}).}

\item{legend}{Legend position, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}.}

\item{reverse_legend}{If \code{TRUE}, reverse the fill legend order via
\code{guide_legend(reverse = TRUE)}. Defaults to \code{FALSE}; useful when
the fill levels were reversed to control the dodge order under
\code{coord_flip()}.}

\item{legend_ncol}{Number of columns to lay the legend keys out in,
passed to \code{guide_legend(ncol = )}. \code{NULL} (default) leaves the
single flush-left column of the house style; \code{2} and up suit a
legend with many short keys, such as binned classes from
\code{\link[=cpb_cut]{cpb_cut()}}, which would otherwise run past the panel.}

\item{zeroline}{If \code{TRUE}, draw a solid black line at zero on the
value axis underneath the boxes. \code{NULL} (default) draws it
automatically when the p5-p95 data spans (or touches) zero, the
house bold-axis-if-zero convention.}

\item{minor, ticks, flush_legend, axis_text_size, legend_key_size, grid_colour, grid_linewidth}{Forwarded to \code{\link[=theme_cpb]{theme_cpb()}} for per-figure deviations from the
house defaults.}

\item{title, subtitle}{Plot title/subtitle.}

\item{xlab, filllab}{Axis and legend title overrides; default
to \code{NULL} (no axis title), matching CPB house style.}

\item{ylab}{Label for the value axis (the \code{y} aesthetic). When
\code{orientation = "horizontal"} it is drawn as the bottom axis title
(after \code{coord_flip()}). When \code{"vertical"}, CPB house style renders
it as the plot \emph{subtitle} -- unless an explicit \code{subtitle} is also
given, in which case it falls back to a rotated y-axis title.}

\item{...}{Further arguments passed to both \code{\link[ggplot2:geom_linerange]{ggplot2::geom_errorbar()}}
and \code{\link[ggplot2:geom_boxplot]{ggplot2::geom_boxplot()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Reproduces the p5/p25/p50/p75/p95 errorbar-plus-boxplot combination
used in CPB distributional figures: a thin errorbar
spanning the p5-p95 range, with a box spanning the p25-p75
interquartile range and a median line at p50 drawn on top. Both
layers use \code{stat = "identity"} -- pass precomputed quantile columns
rather than raw observations.
}
\examples{
library(ggplot2)
df <- data.frame(
  groep = c("laag inkomen", "midden inkomen", "hoog inkomen"),
  p5 = c(-8, -6, -4),
  p25 = c(-4, -3, -2),
  p50 = c(-2, -1, 0),
  p75 = c(0, 1, 2),
  p95 = c(3, 4, 5)
)
cpb_box(df, x = groep, p5 = p5, p25 = p25, p50 = p50, p75 = p75, p95 = p95)
}
