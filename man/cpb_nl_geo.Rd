% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{cpb_nl_geo}
\alias{cpb_nl_geo}
\title{Boundaries of the Netherlands at three administrative levels}
\usage{
cpb_nl_geo(level = c("gemeente", "corop", "provincie"))
}
\arguments{
\item{level}{One of \code{"gemeente"} (municipalities, default),
\code{"corop"} (COROP regions) or \code{"provincie"} (provinces).}
}
\value{
A data.frame with columns \code{code}, \code{name}, \code{part}, \code{ring},
\code{x}, \code{y}.
}
\description{
Returns the bundled generalised boundaries (source: CBS/Kadaster via
cartomap, 2025; Rijksdriehoek coordinates, EPSG:28992) as a flat
polygon table ready for \code{\link[ggplot2:geom_polygon]{ggplot2::geom_polygon()}}: one row per
vertex with the region \code{code} (CBS "statcode", e.g. \code{"GM0014"},
\code{"CR01"}, \code{"PV20"}) and \code{name}, plus \code{part} (polygon-part id, use as
\code{group}) and \code{ring} (ring id within the part, use as \code{subgroup};
rings after the first are holes).
}
\examples{
nl <- cpb_nl_geo("provincie")
head(nl)
}
