% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{cpb_donut}
\alias{cpb_donut}
\title{A CPB-styled donut chart}
\usage{
cpb_donut(
  data,
  fill,
  y,
  label = NULL,
  wedge_labels = TRUE,
  label_style = c("wedge", "leader"),
  label_colour = "black",
  leader_length = 0.15,
  legend_pct = FALSE,
  label_accuracy = 1,
  ring_width = 0.6,
  panel_size = 1.8,
  palette = "qualitative",
  index = NULL,
  reverse_legend = FALSE,
  legend_ncol = NULL,
  legend = "bottom",
  flush_legend = TRUE,
  legend_key_size = NULL,
  title = NULL,
  subtitle = NULL,
  filllab = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table with one row per category.}

\item{fill}{Column mapped to the fill aesthetic (tidy eval), i.e.
the category each wedge represents.}

\item{y}{Column holding each wedge's value (tidy eval). Must be
non-negative -- a wedge with a negative angle has no sensible
meaning. Also drives the percentage shown by \code{wedge_labels} and
\code{legend_pct}: always computed as this row's share of the sum of
\code{y}, regardless of what unit \code{y} itself is in.}

\item{label}{Optional column (tidy eval) holding a ready-formatted
value string per wedge, e.g. \code{"5,1 miljoen"} -- printed as-is, not
derived from \code{y}. \code{NULL} (default): the wedge label is just its
percentage share; supplying \code{label} prints \code{"<label> (<share>\%)"}
instead.}

\item{wedge_labels}{If \code{TRUE} (default), print a value/percentage
label for every wedge (see \code{label}). Either style always sits at
its wedge's own angular midpoint, using the same stacking order
as the wedges themselves, so it can't drift out of sync. With
\code{"wedge"}, thin wedges can still overlap their own label; with
\code{"leader"} the lines instead spread apart automatically (see
\code{label_style}). If \code{FALSE}, \code{legend} may not be \code{"none"} -- with
no values printed anywhere on the plot, the legend becomes the
only way to tell the wedges apart.}

\item{label_style}{Where the label sits: \code{"wedge"} (default,
printed on the wedge itself) or \code{"leader"} (printed just outside
the ring, connected to the wedge by a line). \code{"leader"}'s line
always has two straight parts: a tick perpendicular to the wedge
(starting at the ring's outer edge, see \code{leader_length}), then a
horizontal run out to the label, with every same-side label lined
up at one shared distance. Wedges whose ticks would otherwise
land close enough for their labels to overlap (several thin
wedges bunched together) get their \emph{own} tick extended further
out first, just enough to clear their neighbour, before the
horizontal run -- so \code{"leader"} stays legible with many, or very
unevenly sized, wedges where \code{"wedge"} would not.}

\item{label_colour}{Colour of the printed wedge/leader labels.
Defaults to \code{"black"}; pass \code{"white"} (or another colour) if your
own \code{palette}/\code{index} choice comes out darker/more saturated (for
\code{"wedge"} labels only -- \code{"leader"} labels sit outside the ring,
against the plot background, so contrast doesn't depend on the
wedge colours).}

\item{leader_length}{How far the \code{"leader"}-style line's first,
radial part extends beyond the ring's outer edge, before bending
towards the label, in the same units as \code{ring_width}. This is a
minimum: a tick whose wedge is crowded by a neighbour is extended
further automatically (up to 6x \code{leader_length}) to keep the
labels legible -- capped there because a tick's own physical room
to extend into is itself fixed (see \code{panel_size}), so a longer
cap would just run labels past the figure's own edge instead of
separating them any further. Defaults to \code{0.15}. Ignored when
\code{label_style = "wedge"}.}

\item{legend_pct}{If \code{TRUE}, suffix every legend entry with its
percentage share too, e.g. \code{"gas (45\%)"}. Independent of
\code{wedge_labels} -- either, both, or neither can be on. Defaults to
\code{FALSE}.}

\item{label_accuracy}{Rounding accuracy for every percentage this
function prints (\code{wedge_labels} and \code{legend_pct} alike), passed
to \code{\link[=label_pct_nl]{label_pct_nl()}}. Defaults to \code{1} (whole percentage points).}

\item{ring_width}{Width of the ring, from just over \code{0} (a thin
ring around a large hole) to \code{2} (no hole at all -- a full pie).
Defaults to \code{0.6}.}

\item{panel_size}{The ring's own physical size, in inches (a square
panel; \code{coord_polar()} is aspect-locked so a single number is
enough). Fixed regardless of the title or legend length, so the
same donut always draws the same size -- unlike those, which
overflow past the figure's edge if they do not fit rather than
shrinking the ring to make room. Forwarded to \code{\link[=save_cpb]{save_cpb()}} as its
\code{panel_size} (which can still override it at save time). Defaults
to \code{1.8}.}

\item{palette}{CPB palette to use for \code{fill}; one of
\code{"qualitative"} (default), \code{"discr"}, \code{"sequential"}
(pink ramp), or \code{"blues"} (blue ramp).}

\item{index}{Optional integer vector of palette positions, forwarded
to \code{\link[=scale_fill_cpb_manual]{scale_fill_cpb_manual()}} instead of the default
\code{\link[=scale_fill_cpb_d]{scale_fill_cpb_d()}} when supplied.}

\item{reverse_legend}{If \code{TRUE}, reverse the fill legend order via
\code{guide_legend(reverse = TRUE)}. Defaults to \code{FALSE}.}

\item{legend_ncol}{Number of columns to lay the legend keys out in,
passed to \code{guide_legend(ncol = ...)}. \code{NULL} (default) keeps
ggplot2's own single-row/column layout.}

\item{legend}{Legend position, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}.}

\item{flush_legend, legend_key_size}{Forwarded to \code{\link[=theme_cpb]{theme_cpb()}} for
per-figure deviations from the house defaults. Unlike the other
wrappers, \code{minor}, \code{ticks}, \code{axis_text_size}, \code{grid_colour} and
\code{grid_linewidth} are not exposed here: a donut draws no axis or
gridlines for them to affect.}

\item{title, subtitle}{Plot title/subtitle.}

\item{filllab}{Legend title override; defaults to \code{NULL} (no
title), matching CPB house style.}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_col]{ggplot2::geom_col()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Thin wrapper around \code{\link[ggplot2:geom_col]{ggplot2::geom_col()}} and \code{\link[ggplot2:coord_polar]{ggplot2::coord_polar()}}
with CPB theming and colour scale applied, for a single "share of
total" breakdown: one ring, one wedge per category. Draws no value
axis -- there is nothing on it to label -- so read wedge sizes off
the legend or the data itself.
}
\examples{
df <- data.frame(
  bron  = c("gas", "elektriciteit", "warmte", "overig"),
  share = c(45, 30, 15, 10),
  mld   = c("4,5 mld", "3,0 mld", "1,5 mld", "1,0 mld")
)
cpb_donut(df, fill = bron, y = share, label = mld,
  title = "Energiemix",
  index = c(6, 2, 5, 1)
)
}
