% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{cpb_hist}
\alias{cpb_hist}
\title{A CPB-styled histogram}
\usage{
cpb_hist(
  data,
  x,
  fill = NULL,
  fill_colour = NULL,
  binwidth = NULL,
  bins = NULL,
  outline = "white",
  position = "stack",
  palette = "qualitative",
  fill_index = NULL,
  index = NULL,
  x_lim = NULL,
  x_lim_follow_data = FALSE,
  reverse_legend = TRUE,
  legend_ncol = NULL,
  facet = NULL,
  facet_ncol = NULL,
  facet_scales = "fixed",
  legend = "bottom",
  zeroline = TRUE,
  minor = FALSE,
  ticks = TRUE,
  flush_legend = TRUE,
  axis_text_size = 7,
  legend_key_size = NULL,
  grid_colour = "black",
  grid_linewidth = 0.1,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  filllab = NULL,
  style = c("dutch", "english"),
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table with one row per observation.}

\item{x}{Column with the observations to bin (tidy eval).}

\item{fill}{Optional column mapped to the fill aesthetic (tidy
eval) for grouped histograms; if omitted, bars are drawn in
\code{fill_colour}.}

\item{fill_colour}{Constant bar fill used when no \code{fill} column is
mapped. Defaults to \code{NULL}, which resolves to the CPB primary
blue (\code{cpb_cols(6)}, \code{"#005faf"}).}

\item{binwidth, bins}{Passed to \code{\link[ggplot2:geom_histogram]{ggplot2::geom_histogram()}}; set one
of them (ggplot2 defaults to \code{bins = 30} with a warning
otherwise).}

\item{outline}{Bar outline colour; defaults to \code{"white"}, the house
look for histograms.}

\item{position}{Position adjustment for grouped histograms;
defaults to \code{"stack"}.}

\item{palette}{CPB palette to use for \code{fill}; one of
\code{"qualitative"} (default), \code{"discr"}, \code{"sequential"}
(pink ramp), or \code{"blues"} (blue ramp).}

\item{fill_index}{Which house colours the series get. Either a vector
of palette positions -- \code{c(2, 5, 6)}, forwarded to
\code{\link[=scale_fill_cpb_manual]{scale_fill_cpb_manual()}} -- or a keyword naming a palette:
\code{"discrete"} for the qualitative house palette (blue, magenta,
taupe, ...) and \code{"continuous"} for the sequential ramp. \code{NULL}
(default) uses \code{palette}, which is \code{"discrete"} for every wrapper
except \code{\link[=cpb_map]{cpb_map()}}. A keyword and a non-matching \code{palette} are a
conflict and raise an error, since both set the same thing.}

\item{index}{Deprecated. Former name of
\code{fill_index}. Still accepted, with a warning.}

\item{x_lim}{Optional length-2 vector zooming the \code{x} axis to a
range, without dropping data. Bins are computed from the full
data first, so this only ever changes what is visible, never the
binning itself. \code{NULL} (default) shows the full range.}

\item{x_lim_follow_data}{If \code{TRUE}, remove the default margin on
either side of the \code{x} axis so it sits flush to the data's actual
range, at the cost of ggplot2 picking its own breaks within that
(possibly non-round) range instead of the usual padded, evenly
spaced ones. Matches nicerplot's parameter of the same name.
Defaults to \code{FALSE}. Ignored when \code{x_lim} is set.}

\item{reverse_legend}{If \code{TRUE} (default), reverse the fill legend
order via \code{guide_legend(reverse = TRUE)}.}

\item{legend_ncol}{Number of columns to lay the legend keys out in,
passed to \code{guide_legend(ncol = )}. \code{NULL} (default) leaves the
single flush-left column of the house style; \code{2} and up suit a
legend with many short keys, such as binned classes from
\code{\link[=cpb_cut]{cpb_cut()}}, which would otherwise run past the panel.}

\item{facet}{Optional column (tidy eval) to facet by. Facets follow
the house (legacy nicerplot) convention: the facet title is a bold
strip \emph{below} each panel, and every panel is a complete
mini-figure with its own axes and axis labels.}

\item{facet_ncol}{Number of facet columns, passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{facet_scales}{Whether facet axis ranges are shared; passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} (\code{"fixed"} default, or \code{"free"},
\code{"free_x"}, \code{"free_y"}).}

\item{legend}{Legend position, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}.}

\item{zeroline}{If \code{TRUE} (default), draw a solid black line at
zero on the count axis on top of the bars.}

\item{minor, ticks, flush_legend, axis_text_size, legend_key_size, grid_colour, grid_linewidth}{Forwarded to \code{\link[=theme_cpb]{theme_cpb()}} for per-figure deviations from the
house defaults.}

\item{title, subtitle}{Plot title/subtitle.}

\item{xlab, filllab}{Axis and legend title overrides; default to
\code{NULL}, matching CPB house style.}

\item{ylab}{Label for the count (y) axis, rendered as the plot
\emph{subtitle} (e.g. \code{"aantal"}) unless an explicit \code{subtitle} is
also given.}

\item{style}{Formatting style: \code{"dutch"} (default, \code{.} thousands, \verb{,} decimal)
or \code{"english"} (\verb{,} thousands, \code{.} decimal, English forecast / axis labels).}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_histogram]{ggplot2::geom_histogram()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Thin wrapper around \code{\link[ggplot2:geom_histogram]{ggplot2::geom_histogram()}} with CPB theming
applied: house-blue bars with white outlines, a black zero line and
a count axis that starts on the axis line. Returns a real ggplot
object that can be extended further with \code{+}.
}
\examples{
library(ggplot2)
df <- data.frame(duur = rgamma(1000, 8, 0.6))
cpb_hist(df, x = duur, binwidth = 2,
  title = "Verdeling van de duur",
  ylab  = "aantal",
  xlab  = "duur (maanden)")
}
