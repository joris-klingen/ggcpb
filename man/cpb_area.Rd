% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{cpb_area}
\alias{cpb_area}
\title{A CPB-styled stacked area chart}
\usage{
cpb_area(
  data,
  x,
  y,
  fill,
  palette = "qualitative",
  index = NULL,
  pct_axis = FALSE,
  reverse_legend = TRUE,
  style = c("ggplot", "nplot"),
  legend = NULL,
  zeroline = NULL,
  minor = NULL,
  ticks = NULL,
  flush_legend = NULL,
  axis_text_size = NULL,
  legend_key_size = NULL,
  grid_colour = NULL,
  grid_linewidth = NULL,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  filllab = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table with one row per time x group
combination.}

\item{x, y}{Columns mapped to the x and y aesthetics (tidy eval);
typically a time variable and a value or share.}

\item{fill}{Column mapped to the fill aesthetic (tidy eval), i.e.
the grouping variable being stacked.}

\item{palette}{CPB palette to use for \code{fill}; one of
\code{"qualitative"} (default), \code{"discr"}, or \code{"sequential"}.}

\item{index}{Optional integer vector of palette positions, forwarded
to \code{\link[=scale_fill_cpb_manual]{scale_fill_cpb_manual()}} instead of the default
\code{\link[=scale_fill_cpb_d]{scale_fill_cpb_d()}} when supplied.}

\item{pct_axis}{If \code{TRUE}, format the y axis with \code{\link[=label_pct_nl]{label_pct_nl()}}.}

\item{reverse_legend}{If \code{TRUE} (default), reverse the fill legend
order via \code{guide_legend(reverse = TRUE)}.}

\item{style}{Style preset, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}; see
\code{\link[=cpb_col]{cpb_col()}}.}

\item{legend}{Legend position, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}; \code{NULL}
(default) resolves by \code{style}.}

\item{zeroline}{If \code{TRUE}, draw a solid black line at zero on the
value axis on top of the areas, as the CPB \code{nplot()} house style
does. \code{NULL} (default) resolves by \code{style}.}

\item{minor, ticks, flush_legend, axis_text_size, legend_key_size, grid_colour, grid_linewidth}{Forwarded to \code{\link[=theme_cpb]{theme_cpb()}}; \code{NULL} (the default) resolves by
\code{style}.}

\item{title, subtitle}{Plot title/subtitle.}

\item{xlab, filllab}{Axis and legend title overrides; default
to \code{NULL} (no axis title), matching CPB house style.}

\item{ylab}{Label for the value (y) axis. Following CPB house style
it is rendered as the plot \emph{subtitle} -- a left-aligned italic
caption above the panel -- unless an explicit \code{subtitle} is also
given, in which case it falls back to a rotated y-axis title.}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_ribbon]{ggplot2::geom_area()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Thin wrapper around \code{\link[ggplot2:geom_ribbon]{ggplot2::geom_area()}} with CPB theming and
colour scale applied, for the recurring "share of total over time"
chart.
}
\examples{
library(ggplot2)
df <- data.frame(
  year = rep(2020:2023, each = 2),
  bron = rep(c("gas", "elektriciteit"), 4),
  aandeel = c(60, 40, 55, 45, 50, 50, 48, 52)
)
cpb_area(df, x = year, y = aandeel, fill = bron, pct_axis = TRUE)
}
