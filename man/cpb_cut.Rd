% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cut.R
\name{cpb_cut}
\alias{cpb_cut}
\title{Bin a numeric vector into ordered Dutch class labels}
\usage{
cpb_cut(
  x,
  breaks,
  labeller = label_number_nl(),
  start_label = "lager dan ",
  end_label = " en hoger",
  sep = " - ",
  first = NULL,
  last = NULL,
  ordered = TRUE
)
}
\arguments{
\item{x}{A numeric vector to bin.}

\item{breaks}{A numeric vector of cut points including the outer
bounds, e.g. \code{c(0, 20, 30, 40, 50, 60, Inf)}. As with \code{\link[=cut]{cut()}}, \code{n}
breaks give \code{n - 1} classes. A single integer is passed to \code{\link[=cut]{cut()}}
to request that many equal-width bins (labelled the same way).}

\item{labeller}{A labelling function applied to the break values,
one of the package formatters (\code{\link[=label_number_nl]{label_number_nl()}} (default),
\code{\link[=label_pct_nl]{label_pct_nl()}}, \code{\link[=label_euro_nl]{label_euro_nl()}}) or any \verb{function(x)} returning
a character vector. Only the \emph{finite} interior breaks are labelled;
the open ends use \code{start_label}/\code{end_label}.}

\item{start_label, end_label, sep}{Text around the numbers:
\code{start_label} precedes the first class's upper bound
(\code{"lager dan "}), \code{end_label} follows the last class's lower bound
(\code{" en hoger"}), and \code{sep} joins the two bounds of a middle class
(\code{" - "}).}

\item{first, last}{Optional overrides for the first/last class label
(e.g. \code{first = "geen"}); \code{NULL} (default) uses the constructed
open-ended labels.}

\item{ordered}{If \code{TRUE} (default), return an ordered factor.}
}
\value{
An (ordered) factor the same length as \code{x}.
}
\description{
Cuts \code{x} at \code{breaks} into an ordered factor whose levels read the way
classed CPB figures label them: an open first class
(\code{"lager dan 20\%"}), closed middle classes (\code{"20\% - 30\%"}) and an
open last class (\code{"60\% en hoger"}). Numbers are formatted with a
Dutch-locale formatter, so decimal commas, thousands separators, \verb{\%}
and \verb{€} come out right. Pair with \code{palette = "blues"} in the CPB
scales (or \code{\link[=cpb_map]{cpb_map()}}) for the classed light-to-dark fill.
}
\examples{
aandeel <- c(8, 24, 33, 47, 61, 72)
cpb_cut(aandeel, breaks = c(0, 20, 30, 40, 50, 60, Inf),
        labeller = label_pct_nl())
# -> lager dan 20\% / 20\% - 30\% / ... / 60\% en hoger
}
