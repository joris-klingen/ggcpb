% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{cpb_col}
\alias{cpb_col}
\title{A CPB-styled column (bar) chart}
\usage{
cpb_col(
  data,
  x,
  y,
  fill = NULL,
  fill_colour = NULL,
  group = NULL,
  group_gap = 0.8,
  position = c("stack", "dodge", "fill"),
  orientation = c("vertical", "horizontal"),
  sec_y = NULL,
  sec_type = c("line", "point", "col"),
  sec_limits = NULL,
  sec_label = NULL,
  sec_ylab = NULL,
  sec_colour = NULL,
  sec_linewidth = 0.55,
  sec_points = FALSE,
  sec_point_size = 1.6,
  sec_col_width = 0.3,
  sec_accuracy = NULL,
  palette = "qualitative",
  fill_index = NULL,
  index = NULL,
  pct_axis = FALSE,
  value_accuracy = NULL,
  value_breaks = NULL,
  value_limits = NULL,
  value_labels = FALSE,
  x_lim = NULL,
  x_lim_follow_data = FALSE,
  forecast_x = NULL,
  forecast_label = "raming",
  reverse_legend = TRUE,
  legend_ncol = NULL,
  facet = NULL,
  facet_ncol = NULL,
  facet_scales = "fixed",
  legend = "bottom",
  zeroline = TRUE,
  minor = FALSE,
  ticks = TRUE,
  flush_legend = TRUE,
  axis_text_size = 7,
  legend_key_size = NULL,
  grid_colour = "black",
  grid_linewidth = 0.1,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  filllab = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table with one row per bar segment.}

\item{x, y}{Columns mapped to the x and y aesthetics (tidy eval).}

\item{fill}{Optional column mapped to the fill aesthetic (tidy
eval); if omitted, bars are drawn in a single colour (\code{fill_colour})
with no legend of its own -- unless \code{sec_y} is also set, in which
case the bars still get a one-level legend key (named after \code{y})
so both series are always represented, not just \code{sec_y}. Whenever
\code{sec_y} is set, every fill legend label (real or the one-level
fallback) is suffixed \code{"(linkeras)"} (left axis) to tell it apart
from \code{sec_y}'s own \code{"(rechteras)"} (right axis) key -- house
convention, applied automatically; do not add it to \code{fill}'s own
factor levels yourself.}

\item{fill_colour}{Constant bar fill used when no \code{fill} column is
mapped. Defaults to \code{NULL}, which resolves to the CPB primary blue
(\code{cpb_cols(6)}, \code{"#005faf"}). Ignored when \code{fill} is supplied.}

\item{group}{Optional column (tidy eval) assigning each \code{x} category
to a group, for the published two-level category axis: categories
are laid out in blocks with a gap between groups on \emph{one shared
value axis} (no facets), and the group names are printed in bold
under the category labels. Each category must belong to exactly
one group; groups and categories follow their factor-level order.
The group labels occupy the x-axis-title line, so \code{xlab} is not
available; vertical charts only.}

\item{group_gap}{Gap between group blocks, in category widths;
defaults to \code{0.8}.}

\item{position}{One of \code{"stack"} (default), \code{"dodge"}, or \code{"fill"}.}

\item{orientation}{\code{"vertical"} (default) or \code{"horizontal"} (adds
\code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}} and is forwarded to \code{\link[=theme_cpb]{theme_cpb()}}).}

\item{sec_y}{Optional column (tidy eval) holding a series to draw
against a \strong{secondary value axis} on the right, the house
combination chart (e.g. a stacked wealth total in billions with
the tax raised on it, an order of magnitude smaller, alongside).
One value per \code{x}. Only supported for vertical, ungrouped columns.}

\item{sec_type}{How \code{sec_y} is drawn: \code{"line"} (default), \code{"point"}
(markers only, no connecting line), or \code{"col"} (thin bars,
narrower than the primary columns so the two stay visually
distinct). All three read off the same secondary axis and share
one legend key with the primary fill.}

\item{sec_limits}{Length-2 numeric vector giving the range the
secondary axis spans. \code{NULL} (default) uses zero to the maximum of
\code{sec_y}. \code{sec_y} is placed by mapping this range linearly onto
the primary range, so the two axes always start together.}

\item{sec_label}{Legend label for \code{sec_y}. \code{NULL} (default) uses
the \code{sec_y} column name. Automatically suffixed \code{"(rechteras)"}
(right axis) -- don't add it yourself, e.g. \code{sec_label = "erfbelasting"} shows as \code{"erfbelasting (rechteras)"}.}

\item{sec_ylab}{Unit caption for the secondary axis, drawn
right-aligned above the panel to mirror the left-hand unit that
\code{ylab} puts in the subtitle. \code{NULL} (default) draws none.}

\item{sec_colour}{Colour for \code{sec_y}; defaults to \code{NULL}, which
resolves to the CPB pink (\code{cpb_cols(2)}, \code{"#e6006e"}) that sets it
apart from the blue-led column fills.}

\item{sec_linewidth}{Line width; only used when \code{sec_type = "line"}.
Defaults to \code{0.55}, as in \code{\link[=cpb_line]{cpb_line()}}.}

\item{sec_points}{If \code{TRUE}, add a marker at every point of the
\code{sec_y} line. Only used when \code{sec_type = "line"} -- for markers
without a connecting line, use \code{sec_type = "point"} instead.}

\item{sec_point_size}{Point size; only used when \code{sec_type = "point"}
(the main marker) or \code{sec_type = "line"} with \code{sec_points = TRUE}
(a smaller marker decorating the line, at 0.7x this). Defaults
to \code{1.6}.}

\item{sec_col_width}{Column width; only used when \code{sec_type = "col"},
drawn narrower than the primary bars' own default width (about
\code{0.9}) so the two do not simply overlap. Defaults to \code{0.3}.}

\item{sec_accuracy}{Rounding accuracy for the right-hand axis's own
labels, passed to \code{\link[=label_number_nl]{label_number_nl()}}. \code{NULL} (default) uses that
function's own automatic rounding -- set this when \code{sec_y} needs
a different precision than its default (e.g. whole numbers for a
count alongside a one-decimal percentage share).}

\item{palette}{CPB palette to use for \code{fill}; one of
\code{"qualitative"} (default), \code{"discr"}, \code{"sequential"}
(pink ramp), or \code{"blues"} (blue ramp).}

\item{fill_index}{Which house colours the series get. Either a vector
of palette positions -- \code{c(2, 5, 6)}, forwarded to
\code{\link[=scale_fill_cpb_manual]{scale_fill_cpb_manual()}} -- or a keyword naming a palette:
\code{"discrete"} for the qualitative house palette (blue, magenta,
taupe, ...) and \code{"continuous"} for the sequential ramp. \code{NULL}
(default) uses \code{palette}, which is \code{"discrete"} for every wrapper
except \code{\link[=cpb_map]{cpb_map()}}. A keyword and a non-matching \code{palette} are a
conflict and raise an error, since both set the same thing.}

\item{index}{Deprecated. Former name of
\code{fill_index}. Still accepted, with a warning.}

\item{pct_axis}{If \code{TRUE}, format the value axis with
\code{\link[=label_pct_nl]{label_pct_nl()}}. Uses \code{scale = 100} automatically when
\code{position = "fill"} (proportions), and \code{scale = 1} otherwise
(values already in percentage points).}

\item{value_accuracy}{Rounding accuracy for the value axis labels,
passed to \code{\link[=label_number_nl]{label_number_nl()}} (e.g. \code{0.1} for one decimal place).
\code{NULL} (default) lets \code{scales} pick a sensible accuracy from the
breaks. Cannot be combined with \code{pct_axis}. Use this instead of
adding a second \code{scale_y_continuous()}, which would discard the
wrapper's flush axis (see \code{value_breaks}).}

\item{value_breaks}{Optional breaks for the value axis (passed to
the wrapper-built \code{\link[ggplot2:scale_continuous]{ggplot2::scale_y_continuous()}}). Use this
instead of adding a second y scale, which would discard the
wrapper's axis formatting and expansion.}

\item{value_limits}{Optional length-2 numeric vector giving the
value-axis range (the \code{y} axis, or the flipped axis when
\code{orientation = "horizontal"}). Applied as the wrapper-built value
scale's own \code{limits} (not a coordinate-system zoom), so this is
the hard bound the axis is drawn flush to; a bar/segment that
falls outside it is genuinely dropped, with a warning, the same
as setting \code{limits} on any ggplot2 scale. \code{NULL} (default) flushes
to the full data range instead (see \code{x_lim}/\code{x_lim_follow_data}
for the category axis's equivalent).}

\item{value_labels}{If \code{TRUE}, add \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}} value
labels using \code{y}, positioned to match \code{position}.}

\item{x_lim}{Optional length-2 vector zooming the category (\code{x})
axis to a range, without dropping data -- applied as a
coordinate-system zoom (\code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} /
\code{\link[ggplot2:coord_flip]{ggplot2::coord_flip()}} \code{xlim}), so a bar just outside the window
still contributes to breaks/totals but is only clipped for
display. \code{NULL} (default) shows the full range.}

\item{x_lim_follow_data}{If \code{TRUE}, the category axis sits flush to
the data's own range, with no padding on either side (the bars'
own width is accounted for too, so the outermost bars never spill
past the panel edge). A whole-number \code{x} (almost always a year)
still only ever gets whole-number breaks, never a fractional one.
Defaults to \code{FALSE}: unlike a thin line, a bar's solid fill runs
edge-to-edge, so a flush axis leaves no visual cue for where the
data actually starts and ends -- ggplot2's usual padded, evenly
spaced margin (the default here) keeps that visible. Matches
nicerplot's parameter of the same name. Ignored when \code{x_lim} is
set. Adding your own \code{scale_x_continuous()}/\code{scale_x_discrete()}
afterward replaces this one entirely (ggplot2 keeps only one
scale per aesthetic) -- add \code{expand = ggplot2::expansion(mult = 0)}
to it to keep the flush behaviour when this is \code{TRUE}.}

\item{forecast_x}{Optional x value where the forecast window starts
(vertical charts with a numeric/time x axis). Everything to its
right is overlaid with a translucent white rectangle underneath
the bars and labelled with \code{forecast_label}. Pick a value between
two bars (e.g. \code{2025.5}) so no bar is cut.}

\item{forecast_label}{Label for the forecast window; defaults to
\code{"raming"}. Use \code{NULL} (or \code{""}) for no label.}

\item{reverse_legend}{If \code{TRUE} (default), reverse the fill legend
order via \code{guide_legend(reverse = TRUE)} -- stacking otherwise
makes the legend order counter-intuitive.}

\item{legend_ncol}{Number of columns to lay the legend keys out in,
passed to \code{guide_legend(ncol = )}. \code{NULL} (default) leaves the
single flush-left column of the house style; \code{2} and up suit a
legend with many short keys, such as binned classes from
\code{\link[=cpb_cut]{cpb_cut()}}, which would otherwise run past the panel.}

\item{facet}{Optional column (tidy eval) to facet by. Facets follow
the house (legacy nicerplot) convention: the facet title is a bold
strip \emph{below} each panel, and every panel is a complete
mini-figure with its own axes and axis labels.}

\item{facet_ncol}{Number of facet columns, passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{facet_scales}{Whether facet axis ranges are shared; passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} (\code{"fixed"} default, or \code{"free"},
\code{"free_x"}, \code{"free_y"}).}

\item{legend}{Legend position, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}; accepts
\code{"bottom"} (default), \code{"right"}, \code{"left"}, \code{"top"}, \code{"none"}, or
a two-element numeric vector of plot-relative coordinates.}

\item{zeroline}{If \code{TRUE}, draw a solid black line at zero on the
value axis on top of the bars, as the CPB house style does.
Defaults to \code{TRUE} (bars are anchored at zero).}

\item{minor, ticks, flush_legend, axis_text_size, legend_key_size, grid_colour, grid_linewidth}{Forwarded to \code{\link[=theme_cpb]{theme_cpb()}} for per-figure deviations from the
house defaults.}

\item{title, subtitle}{Plot title/subtitle. \code{subtitle} is normally
left \code{NULL}: CPB house style fills the subtitle line with \code{ylab}
(the value-axis caption). An explicit \code{subtitle} wins, in which
case a vertical chart's \code{ylab} falls back to a rotated y-axis
title.}

\item{xlab}{Label for the \strong{horizontal} axis (bottom, right-aligned
italic). It is attached to the correct ggplot2 aesthetic
automatically: the value (\code{y}) aesthetic when
\code{orientation = "horizontal"} (after \code{coord_flip()}), the category
(\code{x}) aesthetic otherwise.}

\item{ylab}{Label for the \strong{vertical} axis. Following CPB house
style it is rendered as the plot \emph{subtitle} -- a left-aligned
italic caption at the top -- not as a rotated axis title. In a
horizontal bar chart (\code{orientation = "horizontal"}) the vertical
axis is the category axis; in a vertical one it is the value axis.}

\item{filllab}{Legend title override; defaults to \code{NULL} (no legend
title), matching CPB house style.}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_bar]{ggplot2::geom_col()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Thin wrapper around \code{\link[ggplot2:geom_bar]{ggplot2::geom_col()}} with CPB theming and
colour scale applied. Returns a real ggplot object that can be
extended further with \code{+}.
}
\examples{
library(ggplot2)
df <- data.frame(
  year = rep(2021:2023, each = 2),
  group = rep(c("huishoudens", "bedrijven"), 3),
  value = c(10, 15, 12, 18, 14, 20)
)
cpb_col(df, x = year, y = value, fill = group)
}
