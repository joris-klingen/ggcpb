% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{cpb_scatter}
\alias{cpb_scatter}
\title{A CPB-styled scatter plot}
\usage{
cpb_scatter(
  data,
  x,
  y,
  colour = NULL,
  point_colour = NULL,
  size = 0.8,
  palette = "qualitative",
  index = NULL,
  forecast_x = NULL,
  forecast_label = "raming",
  reverse_legend = FALSE,
  facet = NULL,
  facet_ncol = NULL,
  facet_scales = "fixed",
  legend = "bottom",
  zeroline = NULL,
  minor = FALSE,
  ticks = TRUE,
  flush_legend = TRUE,
  axis_text_size = 7,
  legend_key_size = NULL,
  grid_colour = "black",
  grid_linewidth = 0.1,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  colourlab = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table with one row per point.}

\item{x, y}{Columns mapped to the x and y aesthetics (tidy eval).}

\item{colour}{Optional column mapped to the colour aesthetic (tidy
eval). A numeric column gets the continuous CPB gradient
(\code{\link[=scale_colour_cpb_c]{scale_colour_cpb_c()}}); a discrete column gets the discrete CPB
palette. If omitted, points are drawn in \code{point_colour}.}

\item{point_colour}{Constant point colour used when no \code{colour}
column is mapped. Defaults to \code{NULL}, which resolves to the CPB
primary blue (\code{cpb_cols(6)}, \code{"#005faf"}).}

\item{size}{Point size; defaults to \code{0.8}.}

\item{palette}{CPB palette used for a \emph{discrete} \code{colour} column;
one of \code{"qualitative"} (default), \code{"discr"}, or \code{"sequential"}.}

\item{index}{Optional integer vector of palette positions for a
discrete \code{colour} column, forwarded to
\code{\link[=scale_colour_cpb_manual]{scale_colour_cpb_manual()}}.}

\item{forecast_x}{Optional x value where the forecast window
starts; overlaid and labelled as in \code{\link[=cpb_line]{cpb_line()}}.}

\item{forecast_label}{Label for the forecast window; defaults to
\code{"raming"}. Use \code{NULL} (or \code{""}) for no label.}

\item{reverse_legend}{If \code{TRUE}, reverse the colour legend order
via \code{guide_legend(reverse = TRUE)} (discrete \code{colour} only).
Defaults to \code{FALSE}.}

\item{facet}{Optional column (tidy eval) to facet by. Facets follow
the house (legacy nicerplot) convention: the facet title is a bold
strip \emph{below} each panel, and every panel is a complete
mini-figure with its own axes and axis labels.}

\item{facet_ncol}{Number of facet columns, passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{facet_scales}{Whether facet axis ranges are shared; passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} (\code{"fixed"} default, or \code{"free"},
\code{"free_x"}, \code{"free_y"}).}

\item{legend}{Legend position, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}.}

\item{zeroline}{If \code{TRUE}, draw a solid black line at zero on the
value axis underneath the points. \code{NULL} (default) draws it
automatically when the \code{y} data spans (or touches) zero.}

\item{minor, ticks, flush_legend, axis_text_size, legend_key_size, grid_colour, grid_linewidth}{Forwarded to \code{\link[=theme_cpb]{theme_cpb()}} for per-figure deviations from the
house defaults.}

\item{title, subtitle}{Plot title/subtitle.}

\item{xlab, colourlab}{Axis and legend title overrides; default to
\code{NULL}, matching CPB house style.}

\item{ylab}{Label for the value (y) axis. Following CPB house style
it is rendered as the plot \emph{subtitle} -- a left-aligned italic
caption above the panel -- unless an explicit \code{subtitle} is also
given, in which case it falls back to a rotated y-axis title.}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Thin wrapper around \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} with CPB theming and
colour scale applied. Returns a real ggplot object that can be
extended further with \code{+}.
}
\examples{
library(ggplot2)
df <- data.frame(inkomen = rlnorm(100, log(2500), 0.3))
df$energie <- 100 + 0.03 * df$inkomen + rnorm(100, 0, 30)
cpb_scatter(df, x = inkomen, y = energie,
  title = "Energierekening naar inkomen",
  ylab  = "energierekening (euro per maand)",
  xlab  = "besteedbaar inkomen (euro per maand)")
}
