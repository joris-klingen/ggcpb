% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{theme_cpb_min}
\alias{theme_cpb_min}
\title{A minimal CPB theme with no background or gridlines}
\usage{
theme_cpb_min(
  base_family = cpb_font_family(),
  orientation = c("vertical", "horizontal"),
  legend = "right"
)
}
\arguments{
\item{base_family}{Font family for all text. Defaults to
\code{\link[=cpb_font_family]{cpb_font_family()}}, which resolves to \code{"RijksoverheidSansText"}
if the bundled font registered successfully, or \code{""} (the
ggplot2 default family) otherwise.}

\item{orientation}{Either \code{"vertical"} (default; the value axis is
y, e.g. a normal column chart) or \code{"horizontal"} (the value axis
is x, e.g. a chart built with \code{coord_flip()}). Determines which
gridlines the default \code{grid = "value"} shows.}

\item{legend}{Passed through to \code{legend.position}; accepts the
usual \code{"right"}/\code{"left"}/\code{"top"}/\code{"bottom"}/\code{"none"} strings, or a
two-element numeric vector of plot-relative coordinates.}
}
\value{
A ggplot2 \code{theme} object.
}
\description{
A convenience wrapper around \code{\link[=theme_cpb]{theme_cpb()}} with \code{background = FALSE}
and \code{grid = "none"}, useful as a starting point when composing a
custom theme, or for small multiples where gridlines and a filled
background would be too heavy.
}
\examples{
library(ggplot2)
ggplot(mtcars, aes(factor(cyl))) +
  geom_bar() +
  theme_cpb_min()
}
