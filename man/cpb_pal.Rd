% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{cpb_pal}
\alias{cpb_pal}
\title{A CPB colour palette generator}
\usage{
cpb_pal(
  palette = c("qualitative", "discr", "sequential", "blues"),
  reverse = FALSE
)
}
\arguments{
\item{palette}{One of \code{"qualitative"} (the primary 9-colour discrete
palette), \code{"discr"} (the alternate blue/pink-led 7-colour discrete
palette), \code{"sequential"} (the 6-colour light-to-dark pink ramp), or
\code{"blues"} (the 6-colour light-to-dark blue ramp for classed fills).
Ramps are interpolated to any number of levels.}

\item{reverse}{If \code{TRUE}, reverse the palette order before drawing
colours from it.}
}
\value{
A function \verb{function(n)} returning a character vector of
\code{n} hex colours. For \code{"qualitative"} and \code{"discr"}, colours are
recycled (with a warning) if \code{n} exceeds the palette length. For
\code{"sequential"}, \code{n} colours are interpolated along the ramp.
}
\description{
Returns a function of \code{n} that produces \code{n} colours from one of the
three CPB palettes, for use as the \code{palette} argument of a ggplot2
discrete scale. The trailing NA colour (\code{"lightgrey"}) is always
excluded from the cycled colours; use \code{na.value} on the scale
functions to set it instead.
}
\examples{
cpb_pal("qualitative")(3)
cpb_pal("sequential")(5)
cpb_pal("qualitative", reverse = TRUE)(3)
}
