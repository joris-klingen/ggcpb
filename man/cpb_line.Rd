% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{cpb_line}
\alias{cpb_line}
\title{A CPB-styled line chart}
\usage{
cpb_line(
  data,
  x,
  y,
  colour = NULL,
  line_colour = NULL,
  linewidth = 0.55,
  sec_y = NULL,
  sec_limits = NULL,
  sec_label = NULL,
  sec_ylab = NULL,
  sec_linewidth = NULL,
  points = FALSE,
  point_size = 1.1,
  palette = "qualitative",
  colour_index = NULL,
  color_index = NULL,
  index = NULL,
  pct_axis = FALSE,
  value_breaks = NULL,
  value_limits = NULL,
  x_lim = NULL,
  x_lim_follow_data = FALSE,
  ymin = NULL,
  ymax = NULL,
  forecast_x = NULL,
  forecast_label = "raming",
  reverse_legend = FALSE,
  legend_ncol = NULL,
  facet = NULL,
  facet_ncol = NULL,
  facet_scales = "fixed",
  legend = "bottom",
  zeroline = NULL,
  minor = FALSE,
  ticks = TRUE,
  flush_legend = TRUE,
  axis_text_size = 7,
  legend_key_size = NULL,
  grid_colour = "black",
  grid_linewidth = 0.1,
  title = NULL,
  subtitle = NULL,
  xlab = NULL,
  ylab = NULL,
  colourlab = NULL,
  ...
)
}
\arguments{
\item{data}{A data.frame or data.table with one row per x x group
combination.}

\item{x, y}{Columns mapped to the x and y aesthetics (tidy eval).}

\item{colour}{Optional column mapped to the colour aesthetic (tidy
eval); if omitted, a single line is drawn in \code{line_colour}.}

\item{line_colour}{Constant line colour used when no \code{colour} column
is mapped. Defaults to \code{NULL}, which resolves to the CPB primary
blue (\code{cpb_cols(6)}, \code{"#005faf"}). Ignored when \code{colour} is
supplied.}

\item{linewidth}{Line width; defaults to \code{0.55}, matching the
published CPB figures.}

\item{sec_y}{Optional column (tidy eval) holding a series to draw
against a \strong{secondary value axis} on the right, the house
dual-axis line chart (e.g. two rates in \% on the left and an index
on the right). One value per \code{x}. Unlike \code{\link[=cpb_col]{cpb_col()}}, where the
columns key on \code{fill} and the secondary line on \code{colour}, here the
primary series already key on \code{colour}: the secondary line joins
that same scale and takes the next palette position, so all series
share one legend block. House style names the axis in each label,
e.g. \code{"inflatie (linkeras)"} and \code{"reeel loon (rechteras)"}.}

\item{sec_limits}{Length-2 numeric vector giving the range the
secondary axis spans. \code{NULL} (default) uses the range of \code{sec_y}.
The line is placed by mapping this range linearly onto the primary
range, so the two axes always start together.}

\item{sec_label}{Legend label for the secondary line. \code{NULL}
(default) uses the \code{sec_y} column name.}

\item{sec_ylab}{Unit caption for the secondary axis, drawn
right-aligned above the panel to mirror the left-hand unit that
\code{ylab} puts in the subtitle. \code{NULL} (default) draws none.}

\item{sec_linewidth}{Line width for \code{sec_y}; \code{NULL} (default) uses
\code{linewidth}, so the secondary line matches the primary ones.}

\item{points}{If \code{TRUE}, draw a point marker at every observation on
top of the line, the house variant used when the x axis holds a
handful of discrete categories (age brackets, quintiles) rather
than a dense time series. Markers take the line colour, and the
legend key shows a line with a marker on it.
Markers have a radius, so drawing them expands the panel slightly
beyond the data range -- otherwise the first, last and extreme
points are sliced in half by the panel edge. A line on its own
keeps the tight panel, where the axis meets the outermost
gridlines.}

\item{point_size}{Marker size when \code{points = TRUE}; defaults to
\code{1.1}, which reads as the published dot against the default
\code{linewidth}.}

\item{palette}{CPB palette to use for \code{colour}; one of
\code{"qualitative"} (default), \code{"discr"}, \code{"sequential"}
(pink ramp), or \code{"blues"} (blue ramp).}

\item{colour_index}{Which house colours the series get. Either a vector
of palette positions -- \code{c(2, 5, 6)}, forwarded to
\code{\link[=scale_colour_cpb_manual]{scale_colour_cpb_manual()}} -- or a keyword naming a palette:
\code{"discrete"} for the qualitative house palette (blue, magenta,
taupe, ...) and \code{"continuous"} for the sequential ramp. \code{NULL}
(default) uses \code{palette}, which is \code{"discrete"} for every wrapper
except \code{\link[=cpb_map]{cpb_map()}}. A keyword and a non-matching \code{palette} are a
conflict and raise an error, since both set the same thing.}

\item{color_index}{American-spelling alias for \code{colour_index}; ignored
when \code{colour_index} is given.}

\item{index}{Deprecated. Former name of
\code{colour_index}. Still accepted, with a warning.}

\item{pct_axis}{If \code{TRUE}, format the y axis with \code{\link[=label_pct_nl]{label_pct_nl()}}.}

\item{value_breaks}{Optional breaks for the value axis (passed to
the wrapper-built \code{\link[ggplot2:scale_continuous]{ggplot2::scale_y_continuous()}}). Use this
instead of adding a second y scale, which would discard the
wrapper's axis formatting and expansion.}

\item{value_limits}{Optional length-2 numeric vector giving the
value-axis range, applied as the wrapper-built value scale's own
\code{limits} (not a coordinate-system zoom) -- the hard bound the axis
is drawn flush to; a point outside it is genuinely dropped, with a
warning, the same as setting \code{limits} on any ggplot2 scale. \code{NULL}
(default) flushes to the full data range instead.}

\item{x_lim}{Optional length-2 vector zooming the \code{x} axis to a
range, without dropping data -- applied as a coordinate-system
zoom (\code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} \code{xlim}). \code{NULL} (default) shows
the full range.}

\item{x_lim_follow_data}{If \code{TRUE}, remove the default margin on
either side of the \code{x} axis so it sits flush to the data's actual
range, at the cost of ggplot2 picking its own breaks within that
(possibly non-round) range instead of the usual padded, evenly
spaced ones. Matches nicerplot's parameter of the same name.
Defaults to \code{FALSE}. Ignored when \code{x_lim} is set.}

\item{ymin, ymax}{Optional columns (tidy eval) bounding an
uncertainty band, drawn as a translucent ribbon underneath the
line(s). With a \code{colour} mapping each series gets a band in its
own colour; otherwise the band uses the line colour.}

\item{forecast_x}{Optional x value where the forecast window
starts. Everything to its right is overlaid with a translucent
white rectangle (drawn underneath the data) and labelled with
\code{forecast_label}, the house convention for marking predicted
values.}

\item{forecast_label}{Label for the forecast window; defaults to
\code{"raming"}. Use \code{NULL} (or \code{""}) for no label.}

\item{reverse_legend}{If \code{TRUE}, reverse the colour legend order
via \code{guide_legend(reverse = TRUE)}. Defaults to \code{FALSE}: unlike
the stacked wrappers, line order carries no stacking convention.}

\item{legend_ncol}{Number of columns to lay the legend keys out in,
passed to \code{guide_legend(ncol = )}. \code{NULL} (default) leaves the
single flush-left column of the house style; \code{2} and up suit a
legend with many short keys, such as binned classes from
\code{\link[=cpb_cut]{cpb_cut()}}, which would otherwise run past the panel.}

\item{facet}{Optional column (tidy eval) to facet by. Facets follow
the house (legacy nicerplot) convention: the facet title is a bold
strip \emph{below} each panel, and every panel is a complete
mini-figure with its own axes and axis labels.}

\item{facet_ncol}{Number of facet columns, passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}

\item{facet_scales}{Whether facet axis ranges are shared; passed to
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} (\code{"fixed"} default, or \code{"free"},
\code{"free_x"}, \code{"free_y"}).}

\item{legend}{Legend position, forwarded to \code{\link[=theme_cpb]{theme_cpb()}}.}

\item{zeroline}{If \code{TRUE}, draw a solid black line at zero on the
value axis underneath the data lines. \code{NULL} (default) draws it
automatically when the \code{y} data spans (or touches) zero, the
house bold-axis-if-zero convention.}

\item{minor, ticks, flush_legend, axis_text_size, legend_key_size, grid_colour, grid_linewidth}{Forwarded to \code{\link[=theme_cpb]{theme_cpb()}} for per-figure deviations from the
house defaults.}

\item{title, subtitle}{Plot title/subtitle.}

\item{xlab, colourlab}{Axis and legend title overrides; default
to \code{NULL} (no axis title), matching CPB house style.}

\item{ylab}{Label for the value (y) axis. Following CPB house style
it is rendered as the plot \emph{subtitle} -- a left-aligned italic
caption above the panel (e.g. the unit, \code{"\%"}) -- unless an
explicit \code{subtitle} is also given, in which case it falls back to
a rotated y-axis title.}

\item{...}{Further arguments passed to \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Thin wrapper around \code{\link[ggplot2:geom_path]{ggplot2::geom_line()}} with CPB theming and
colour scale applied.
}
\examples{
library(ggplot2)
df <- data.frame(
  jaar = rep(2018:2022, 2),
  raming = rep(c("CEP", "MEV"), each = 5),
  bbp_groei = c(2.1, 1.8, -3.8, 4.9, 4.5, 2.4, 1.6, -3.6, 4.6, 4.2)
)
cpb_line(df, x = jaar, y = bbp_groei, colour = raming)
}
